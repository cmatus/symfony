{{ form_start(form) }}
    
    {{ form_widget(form) }}
    
    <input type="submit" value="{{ 'Borrar Cliente'|trans }}" class="btn-bg" onclick="return confirm('Estas seguro?')" >
        
{{ form_end(form) }}

